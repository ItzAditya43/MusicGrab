[package]
name = "musicgrab"
version = "1.0.0"
description = "MusicGrab desktop app"
authors = ["ItzAditya43"]
license = "GPL-3.0-or-later"
repository = "https://github.com/ItzAditya43/MusicGrab"
edition = "2021"
rust-version = "1.77.2"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[lib]
name = "app_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2.6.3", features = [] }

[dependencies]
serde_json = "1.0"
serde = { version = "1.0", features = ["derive"] }
log = "0.4"
tauri-plugin-log = "2"

# Tauri's build script cross-checks the `tauri` crate's enabled features
# against what each platform's config actually declares and errors on a
# mismatch either way — so `protocol-asset` (needed for Android's
# security.assetProtocol, used to stream on-device downloaded files into
# <audio>) has to be enabled only for that target, not globally.
[target.'cfg(not(any(target_os = "android", target_os = "ios")))'.dependencies]
tauri = { version = "2.11.3", features = [] }
tauri-plugin-shell = "2"

[target.'cfg(target_os = "android")'.dependencies]
tauri = { version = "2.11.3", features = ["protocol-asset"] }
tauri-plugin-musicgrab-ytdl = { path = "../plugins/tauri-plugin-musicgrab-ytdl" }
